# Scripts

## Create Screenshots GIF

To generate `docs/screenshots.gif` from the README images (makes the README shorter):

```sh
pip install pillow requests
python scripts/create_screenshots_gif.py
```

The GIF will be saved to `docs/screenshots.gif`.
